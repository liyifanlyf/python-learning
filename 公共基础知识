1.1 计算机概述
计算机的工作就是自动快速地执行程序
程序的执行过程可分为取指令，分析指令和执行指令
软件是程序，数据和文档的集合

1.2 计算机硬件系统
机器周期的同步标准是CPU访问存储器一次所需要的时间
总线带宽是指总线的数据传输率
虚拟存储器是对主存的逻辑扩展虚拟存储器的空间大小取决于计算机的访存能力而不是实际外存的大小
理论上计算机虚拟内存最大容量取决于计算机地址位数
计算机所支持的最大内存是由该计算机的地址位数决定的，也就是计算机的最大寻址能力

1.3 数据的内部表示
计算机中存储和运算通常采用的格式是补码

1.4 操作系统
允许多个联机用户同时使用一台计算机系统进行计算机的操作系统称为分时操作系统
进程调度就是按一定策略动态地把CPU分配给处于就绪队列中的某一进程并使之执行的过程
计算机完成一条指令所花费的时间称为一个指令周期
进程是可以并发执行的程序的执行过程，它具有动态性，共享性，独立性，制约性和并发性五种属性
过程控制系统，信息查询系统和事务处理系统都属于实时系统
一个正在运行的进程由于所申请的资源得不到满足要调用阻塞进程原语
当进程在运行状态下结束要调用撤销进程原语
唤醒进程原语是把进程从等待队列里移出到就绪队列并设置进程为就绪状态
当一个进程在运行过程中释放了系统资源后要调用创建进程原语
多道程序环境下单CPU的计算机允许多个进程并发执行
将文件名转换为文件存储地址的结构是文件目录
当CPU需要执行I/O操作时，程序将主动查询I/O设备是否准备好

2.1 算法
设计算法时不仅需要考虑数据结构的设计——>决定数据的输入    #还要考虑结果的可靠性，时间复杂度
顺序程序具有顺序性，封闭性和可再现性
算法的复杂度是指运行该算法所需要的计算机资源是多少
算法的时间复杂度与空间复杂度没有直接关系
对数据进行压缩存储会降低算法的空间复杂度

2.2 数据结构的基本概念
数据结构是相互之间有关联的数据元素的集合，即数据的组织形式
1）数据是指所有能输入到计算机中并被计算机程序处理的符号的总称
2）数据元素是指数据的基本单位，在计算机程序中通常作为一个整体进行考虑和处理
3）一个数据元素可以由若干个数据项组成，数据项是数据的最小单位
表示数据结构的方法：1.二元元素表   2.图形表示
从逻辑关系上描述数据，它与数据的存储无关，是独立于计算机的
栈——后进后出
队列——后进后出
四种存储方式：
1.顺序存储结构
2.链式存储结构
3.索引结构
4.散列存储方式
数据的运算：检索，排序，插入，删除，修改等
对于同一个逻辑结构来说，采用不同的存储结构时，其数据处理的效率是不同的

2.3 线性表及其顺序存储结构
线性表的链式存储结构所需要的存储空间一般要多于顺序存储结构
线性表的概念是从逻辑结构的角度说的。所以，判断是否为线性表时并不考虑其存储结构
在顺序表中，每个元素占有相同的存储单位
在线性表的顺序存储结构中可以计算各数据元素（数据起点）的起始地址
顺序表的插入计算的步骤：1）判断是否上溢：首先判断为线性表开辟的存储空间已满，如果已满则不能插入，未满就继续第二步
                      2）空出第一个位置：从最后一个元素开始到插入的位置上的元素，将其中的每个元素均依次往后移动一位
                      3）插入：把新元素放入所插入的位置

2.4 栈和队列

1）栈：
栈和队列都是顺序存储空间
在程序设计语言中，与普通线性表一样，用一堆数组作为栈是s(1:m)的顺序存储结构，其中m为栈的最大容量
栈中不允许插入和删除的另一端称为栈底，允许插入和删除的一端称为栈顶
当top=0时为空栈，当top=m（最大容量）则为栈满
入栈步骤：
  首先，判断栈是否为满，如果满则不能入栈（top=m）
  其次，将栈顶指针进一（即top加1）

退栈：
  首先，判断top是否为0
  其次，将栈顶元素赋值给一个制定的变量
  最后，栈顶指针退一（top减一）
应避免“下溢”错误出现

读栈顶元素：
  将栈顶元素赋值给一个制定的变量
  第一，栈顶指针不会变
  第二，top=0时，读不到栈顶元素

上溢错误：当栈顶指针指向最后最后一个位置，说明栈的空间已满

2）队列
队列原则： 1）初始时该队列为空
          2）当有用户程序到来时，将该用户程序加入到队伍末尾进行等待
          3）当计算机系统执行完当前的用户程序后，就从队列头部取出一个用户程序来执行

顺序结构一般采用循环队列的形式
原理：循环队列是指当队列存储空间的最后一个位置已被使用而仍要进行入队运算，这时只要存储空间的第一个位置空闲，便可以将元素加入这个位置，即将存储空间的第一个位置作为队尾
在循环队列中元素无法根据指针位置确定
在循环队列中如果front=rear可能是满或空
如果front<rear长度为:rear-front
如果front>rear长度为:rear-front+m
在长度为n的线性表中寻找值最大的元素，最大的情况下需要比较的次数为n-1

2.5 线性链表
顺序：
链式存储的存储单元是不连续的，因为是不连续的存储空间，所以指针将不会规律地运动
线性链表适合大的线性表或者变动频繁的线性表
单链表的缺点是只能找到后件，不能找到前件
1）链式存储方式中的存储节点不一定是连续的
2）各节点的存储顺序与数据元素之间的逻辑关系可以不一致。
3）链式存储方式可以用于线性结构，也可用于非线性结构。
循环：
在循环队列中，队头指针和队尾指针的动态变化决定队列的长度
3项基本运算：查找，插入，删除
无条件时：当状态不为front=rear=NULL时，如果front=rear，则队列中的元素个数为1
有条件时：当状态不为front=rear=NULL时，如果front=rear，则队列中的元素个数为0或最大容量
元素值x的前一个节点P的基本方法：当线性链表中存在包含元素x的节点时，则找到的P为第一次遇到包含元素x的前一个节点序号
对于节点的删除几种情况：
1）位空格，无节点删除
2）删除第一个节点，头指针指向第二个节点
3）删除最后一个节点
4）删除链表中的节点
循环链表：表头其数据域为任意或根据需要来设置指针域为指向线性表的第一个元素的节点

2.6 树的基本概念
树的总结点数为度3的节点数+度为2的节点数+度为1的节点数+度为0的节点数
树的总结点数为树中所有节点的度数之和再加一
二叉树中度为0的节点总比度为2的节点多一个
顺序存储结构：用一组连续的存储单位存放二叉树中的节点
链式存储结构：用链表来表示一颗二叉树，即用链来表示着元素的逻辑关系。
通常采用二叉链表存储式

2.7 查找技术
查找的速率影响数据处理的速率
顺序查找的最好的查询次数是指所查找的元素是线性表的第一个元素时的查询次数

2.8 排序技术
排序方法：交换类排序，
         插入类排序，
         选择类排序
交换类排序：
    2.快速排序：
      将原问题分解成若干个小规模的同结构的问题，递归解决每个子问题。
      快速排序每一次交换，都会产生逆序
插入类排序法：
    1.简单插入排序法：
      将无序列表中的各元素依次插入到已有序的线性表中。

3.1 程序设计方法与风格
程序设计：设计，编制，调试程序的方法和过程
程序设计风格：编写程序时所表现出的特点，习惯和逻辑思路
总体原则：清晰第一，效率第二，简单易懂
1）源程序文档化：
    1）符号名的命名：符号名的命名应具有一定的实际含义，以便于对程序功能的理解
    2）程序注释：.序言性注释位于程序的起始部分，说明整个程序模块的功能。
                .功能性注释一般嵌套在源程序体内，主要描述相关语句或程序段的功能。
    3）视觉组织：通过在程序中添加一些空格，空行和缩进等，使人们在视觉上对程序的结构一目了然
2）数据说明的方法：
      规范化，有序化，使用注释
3）语句结构：
      清晰第一，效率第二，简单易懂
4）输入和输出：
      方便用户的使用原则

3.2 结构化程序设计
1）结构化程序设计方法的重要原则：1）自顶向下：先从取上层总目标开始设计
                              2）逐步求精：逐步使问题具体化
                              3）模块化：把程序要解决的总目标分解为分目标，再进一步分解为具体的小目标
                              4）限制使用goto语句：为了程序清晰性
2）结构化程序的基本结构与特点：
        1）顺序结构
        2）选择结构（if...else结构）
        3）重复结构（while或until）：优点：1）程序易于理解，使用和维护。
                                         2）提高了编程工作的效率，降低了软件开发成本
                                    缺点：对于大型，复杂的软件，由于其中内在的高耦合，低内聚，特别是当时团队开发的管理方法的不足，难以对系统进行维护

3.3 面向对象的程序设计
面向对象方法是一种运用对象、类、继承、封装、聚合、关联、消息、多态性等概念来构造系统
面向对象的方法与技术开始走向实用的标志性语言是smalltalk
传统结构化程序设计方法的核心是算法，面向对象的核心是对像（类）
1）面向对象方法的基本概念：1）与人类思维习惯一致
                         2）稳定性好
                         3）可重用性好
                         4）易于开发大型软件产品
                         5）可维护性好
属性：属性是指事物的特性，表示事物的静态特征
操作：操作是指事物的功能，表示事物的动态特征
2）类和实例类：具有共同属性、共同方法的对象的集合，是关于对象的抽象描述。
对象和实例是两种不同的概念
类可以用来创建对象，对象是类的一个实例
3）封装：实现信息隐蔽，尽可能隐蔽对象的内部细节，但降低了对象键耦合度
耦合度是指对象和对象之间联系的紧密程度。对象之间的联系越紧密，其耦合度越大
4）消息
消息传递是对象间通信的手段，它请求对象执行某一处理或回答某一要求的信息
5）继承
如果类A具有类B的全部属性和全部服务，而且具有自己特有的某些属性或服务，则A叫做B的特殊类，B叫做A的一般类
原类与新类的关系是一般类与特殊类的关系
继承是一种在多个类之间共享属性和操作的机制
单继承
多重继承
6）多态性
多态性：同样的消息被不同的对象接收时可导致完全不同的行为。可以通过方法重载和方法重写来实现多态
利用多态性，用户能够发送一般形式的消息，而将所有的实现细节都留给接收信息的对象

4.1 软件工程基本概念
2）软件危机：
  主要在成本，质量和生产率等几个方面
3）软件工程：
  软件工程三要素：包含方法、工具和过程3个要素
  试图用工程、科学和数学的原理与方法研制、维护计算机软件的有关技术及管理方法
  软件开发技术的主体内容是软件开发方法学
  软件工程有八大原则：抽象，信息隐蔽，模块化，局部化，确定性，一致性，完备性，可验证性
5）软件生命周期：
  3）需求分析：解决做什么
  4）软件设计：解决怎么做
