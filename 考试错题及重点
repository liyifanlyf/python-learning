除了夸Python语言执行快是错的，其他夸Python语言的都是对的，系统软件的要求是执行速度快，所以Python不适合编写系统软件。

unexpected indent 错误代表缩进错误

三引号注释本质是创建一个没有赋值的字符串

Python函数的返回值使用很灵活，可以没有返回值，可以有一个或多个返回值

序列类型是一维元素向量，元素之间存在先后关系，通过序号访问。其中字符串，列表，元组是序列类型。字典是映射类型。

ord("A")=65

writelines()方法是将一个元素为字符串的列表整体写入文件

二维列表对象输出为CSV格式文件采用遍历循环和字符串的join()方法相结合

一维数据用列表（list）形式表示，集合（set）表示不了    #考试重点#

要输出文件内容不能用默认模式，要用read（）模式

字符串形式可以代替#号

（ABC）等于括住了元素，算是违规了变量名；实际创建了ABC

只有下划线可以用作变量命名，（）也可以

int("100/3")这种会有转换错误，会报错。哪怕是1.0都会报错

整数类型可以用二进制、八进制、十进制、十六进制来表示

5%6取余为5，10//8取整为1

Python是脚本语言，用解释方式执行。解释过后生成目标代码

print，eval保留字，这种后面加（）的，都不是保留字

await是用作异常处理的保留字
保留字有35个：and,as,assert,async,await,break,class,continue,def,del,elif,else,except,False,finally,for,from,global,if,import,in,is,lambda,None,nonlocal,not,or,pass,return,Turn,try,with,while,yield,raise.

pass是用来补全代码格式的

#注释可以放在任何地方，三引号注释只能放在开头

a,b=1,2或(a,b)=(1,2),这种行为叫作元组赋值

if,while,def,class最后面都要用‘：’结尾      #特别是class要记住

想要输出文件的内容，需要把文件的内容读入，如txt1=txt.read()

writelines()方式写入文件，会把列表格式去掉，没有间隔

0.1+0.2==0.3，输出结果是False，因为0.1+0.2==0.30004
