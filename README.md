# 我的 Python 学习仓库
从今天开始，每天把练习代码推到这里。
#1.1 程序设计语言：
#机器语言：由0,1组成；执行速度极快，不需要翻译
#汇编语言：由符号语言，单词，字母组成；需要翻译
#高级语言：有Python，C语言等；执行速度，相对较慢
#静态语言使用编译方式执行：包含C语言，JAVA
#脚本语言使用解释方式执行：包含Python
#编译是将源代码（高级语言代码）转换成目标代码的过程
#解释是将源代码逐条转换成目标代码并同时逐条运行的过程

#1.2 Python语言
#创始人是吉多.范罗苏姆
#Python2于2000年10月16日发布      Python3于2008年12月3日发布
#系统软件一般使用静态语言编写
#Python最重要的三个特点：语法简洁，生态丰富，多语言集成，平台无关，强调可读，支持中文，模式多样，类库便捷
#Python语言发展的主要原因：语言开源开放，能够整合（语言代码）；各领域应用广泛，通用性强；语法简洁，精炼。语言设计质量高

#1.3 Python安装
#运行Python程序的关键是安装Python的解释器
#下载网址：http://www.python.org/downloads
#IDLE为Python的环境   shell意为即时运行
#File为文件
#改后缀名为.py等
#编写点Edit with IDLE
#Ctrl+w    在IDLE交互界面下，启动编辑器
#Ctrl+[    减少缩进代码
#Ctrl+]    增加缩进代码
#Alt+3     注释代码行
#Alt+4     取消注释代码行
#Alt+/     单词完成，只要文中出现过，就可以帮你自动补齐
#F5        在IDLE编辑器中运行Python程序

2.1 基本语法
1）缩进：表示Python间的包含和层次关系
  用在if，while，for，def，class等关键字里面
2）注释：
  注释会被编译器或解释器略去
  文档注释是三个单引号或三个双引号（可以多行注释）
  注释能用#，也可以用引号
3）续行符：
  续行符用\表示。可以用于单行代码较长的情况，也适合对代码进行多行排版增加可读性的情况。（用了必须直接换行）

3.5 format（）方法的基本使用
1.format（）方法的用法
  作用：格式化字符串，用于解决字符串和变量同时输出的格式安排问题
  语法:<模板字符串>.format(<逗号分隔的参数>)
  说明：模块字符串是一个由字符串和槽组成的字符串
2.format()方法的格式控制
  format()的槽不仅包括参数序号还包括格式控制信息
  语法格式：{<参数序号>:<格式控制标记>}
  <等于左对齐；>右对齐；^居中对齐
3.6 类型判断与类型间的转换
类型判断函数：
    type(x)：对x变量进行类型判断
类型转换函数： int(x):将x转换为整数
              float(x):将x转换为浮点数
              str(x):将x转换为字符串
              
4.1 程序流程图
程序流程图是一种表达程序控制结构的方式。由一系列图形，流程线和文字说明等组成。流程图的七种元素如下：
1）起止符：表示程序逻辑的开始或结束
2）判断框：表示一个判断条件，并根据判断结果选择
3）处理框：表示一组处理过程，对应顺序执行的程序；逻辑
4）输入/输出框：表示程序的数据输入或结果输出
5）注释框：表示程序的注释
6）流向线：表示程序的控制流，以带箭头直线或曲线表达程序的执行路径
7）连接点：表示多个流程图的连接方式，常用于将多个较小流程图组织成较大流程图
描述一个计算问题的程序过程有多种方式，包括：IPO，流程图，伪代码或程序代码。其中，流程图采用图形方式最为直观，这里仅使用这种形式介绍3种控制结构的基本和扩展含义。
程序由3种基本结构组成：顺序结构，分支结构和循环结构
顺序结构是程序按照代码的编写顺序依次执行的一种运行方式

4.2 分支结构
1）单分支结构：
  分支结构是程序根据条件判断结果而选择不同向前执行路径的一种运行方式
语法格式：
  if<条件>：
    <代码块>
2）二分支结构：
  语句1是在条件为真时执行，语句2是在条件为假时执行。
语法格式：
  if<条件>：
    <代码块1>
  else:
    <代码块2>
3）多分支结构：
  多分支结构一般用于判断同一个条件或一类条件的多个执行路径。依次判断条件并执行第一个条件为True的语句。如果没有条件成立，执行else下面的语句。
if<条件>：
    <代码块1>
elif:
    <代码块2>
elif:
    <代码块3>
…………
else:
    <代码块N>
4）分支嵌套：
age=50
if age<18:
  print("未成年")
else:
  if 18<=age<30:
    print("年轻人")
  elif 30<=age<=60:
    print("中年人")
  else:
    print("老年人")

4.3 程序的循环
1.for循环：
    从遍历结构中逐一提取元素，放在循环变量中，对于每个提取的元素执行一次语句块。for语句的循环执行次数是根据迭代器中元素个数确定的。
语法格式：
  for<>in<>:
    <>
遍历结构可以是字符串，文件，range()函数或其它组合数据类型等。

range()函数的使用方式：
从a（包含）开始到b（不包含）结束，以c为差形成等差数列
for循环经常和range()函数一起使用，基本使用方式如下：
  for<循环变量>in range(<循环次数>):
      <语句块>
2.while循环
  程序执行while语句时，判断条件，如果为True，执行循环体语句，语句结束后返回再次判断条件；直到条件为False时，循环终止，执行与while同级别的后续语句。
语句格式：
    while<条件>:
      <代码块>
4.4 循环扩展及控制语句
1、循环扩展模块：
  当while循环正常执行之后，程序会绝续执行else语句中内容。else语句只在循环正常执行之后才执行并结束。
  语法格式：
            while<条件>：
                <语句块1>
            else:
                <语句块2>

2.循环控制语句：
1）continue语句：
  用来结束当前当次循环，即跳出循环体中下面尚未执行的语句，但不跳出当前循环。
2）break语句：
  跳出最内层循环，终止该循环后，从循环后的代码继续执行。


4.5 程序的异常处理
>>>3/0
ZeroDivisionError:division by zero
>>>"x"+1
TypeError:Can't convert 'int' object to str implicitly
>>>{1,2,3}*2
TypeError:unsupported operand type(s) for*: 'set' and 'int'
>>>print(name)
NameError:name 'name' is not defined
>>>f=open("wangyan.txt","r")
FileNotFoundError:[Errno2] No such file or directory:'wangyan.txt'
>>>len(10110234)
TypeError:object of type 'int' has no len()
ValueError:类型对但值无效 如：int("abc")
1.try...except:
    Python处理异常程序最简单的形式就是try...except...,类似于单分支选择结构。使用语法为：
  try：
      #可能会发生异常的代码，先执行以下试试看
  except<异常类型>：
      #如果try中代码抛出异常并被except捕获，执行此处代码
2.try...except...else...:
    该结构可以看为双分支结构，else中的代码只有在try中代码没有引发异常的情况下执行
使用语法为：
        try：
          #可能会发生异常的代码
        except<异常类型>：
          #如果try中代码抛出异常并被except捕获，执行此处代码
        else：
          #try代码正常执行，则执行此处代码
3.try...except...finally...
  该结构中，无论try代码中是否发生异常，finally语句块中的代码总会执行。
使用语法为：
        try：
          #可能会发生异常的代码
        except<异常类型>:
          #处理异常代码
        finally：
          #总是执行

5.1 列表
列表：有序，可变
元组：有序，不可变
字符串：有序，不可变
字典：无序，可变
集合：无序，可变
id（）  #返回变量的内存地址
1.定义： 列表的所有元素放在一对[]内，相邻元素之间用逗号分隔；
        列表没有长度限制，元素类型可以不同，不需要预定以长度
        列表内元素有顺序，可以使用索引；
        线性的数据结构；
        列表是可变的

2.列表的创建：
创建空列表：L=[]；
           L=list()
创建非空列表：L=[1,2,3,4]
             L=[1,2,[3.1,3.2,3.3],4]
             list('adcf')

3.列表的访问：
    列表L=['p','y','t','h','o','n']

4.列表的操作方法：
append（）        在列表的末端添加新的元素
count（value）    统计元素在列表中出现的次数
clear（）         清除列表中出现的次数
extend（iterable）在列表末尾一次性追加另一个序列中的多个值
insert（index，x）将元素插入列表的指定索引位置处
index（）         从列表中找到第一个匹配项的索引位置
pop（[index]）    从列表中移除一个元素并返回该元素的值
remove（value）   移除列表中的第一个匹配项
reverse（）       将列表中的元素反转
copy（）          对原列表进行复制，生成一个新的列表
#copy（）和=并不一样；如果用clear清空，用=复制的会一同清空，用copy（）复制的却不会，因为copy（）是重新创建一个内存，而=是共用一个内存
len（）    返回列表中的元素个数
min（）    返回列表中的最小元素
max（）    返回列表中的最大元素
list（）   将一个序列转换为列表

5.列表支持的运算符
+：结果生成新的列表
+=：结果不生成新的列表
*：结果生成新的列表
*=：结果不生成新的列表


5.2 元组（tuple）
1.定义：
  所有元素放在圆括号（）内；
  如果元组中只有一个元素，必须在最后增加一个逗号；
  使用tuple创建空元组；
  支持双向索引；
  元组是不可变的。
列表：可变序列，可修改，不可作为字典的键
元组：不可变序列，不可修改，可作为字典的键
两者共同点：有序序列，双向索引。通过切片访问，del删除


5.3 字典（dict）
1.定义
  字典是可变的，可以存储任意类型
  字典用花括号{}表示
  键和值通过冒号连接组成的集合
          {<键1>:<值1>,<键2>:<值2>,...<键n>:<值n>}
  字典中是以键值对存储的，键值对没有顺序且不能重复
  元素类型可以不同
  dict（）生成一个空字典
  键就是值的索引
2.字典的索引：
  1）访问值：
      <值>=<字典变量>[<键>]
  2）修改值：
      <字典变量>[<键>]=<值>
  3）增加元素：
      <字典变量>[<新键>]=<键>
3.字典的操作方法
d.keys（）              返回字典中所有的键信息
d.values（）            返回字典中所有的值信息
d.items（）             返回字典中所有的键值对（键值对以元组类型表示）
d。get（key，default）  键存在则返回相应值，不存在则返回默认值default
d。clear（）            删除所有键值对，清空字典
d.popitem（）           随机从字典取出一个键值对，以元组形式返回，同时将该键值对从字典中删除
d。pop（key，default）  键存在则返回相应值，同时删除键值对，否则返回默认值default
  
